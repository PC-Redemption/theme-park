# Settings Portal Static

Settings Portal Static is the framework-neutral starter for admin, health, and configuration workflows.

It exists to prove that the shared design system also supports table-heavy settings portals without requiring a server-side template runtime.
